{
  // Suite config: each listed JSONC is run through the shared `openmodalpy run` core.
  "kind": "config-suite",
  "name": "OpenModalPy canonical example suite",
  "description": "Run the canonical OpenModalPy examples: cavity, cylinder, and jet.",
  "configs": [
    "./cavity.jsonc",
    "./cylinder.jsonc",
    "./jet.jsonc"
  ]
}
